require 'spec_helper'

describe Katex2html do
  it 'has a version number' do
    expect(Katex2html::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
